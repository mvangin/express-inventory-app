extends layout

block content 
    h1= title
    ul 
        each brand in brandList 
            li 
                a(href=brand.url) #{brand.name}
                br
                br
    a(href="/catalog/brand/create") New Brand   
