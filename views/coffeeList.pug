extends layout

block content 
    p= title
    ul 
        each coffee in coffeeList 
            li 
                a(href=coffee.url) #{coffee.name}
                | 
                p #{coffee.categories.name}