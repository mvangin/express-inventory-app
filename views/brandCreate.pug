extends layout

block content
    form(method="post")
        label(for="name") Name
            input(name="name" type="string" value="")
        label(for="description") Description
            input(name="description" type="string" value="")
        button(type="submit" action="/catalog/brand/create") Create